import React from "react";
import ReactDOM from "react-dom";
import Cartainteractiva from "./Cartainteractiva"; // Importa el componente

ReactDOM.render(
  <React.StrictMode>
    <Cartainteractiva />
  </React.StrictMode>,
  document.getElementById("root")
);
